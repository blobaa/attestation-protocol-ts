{
  "name": "@project-ap/attestation-protocol-ts",
  "version": "1.0.0",
  "description": "An implementation of the Attestation Protocol written in TypeScript",
  "main": "dist/src/index.js",
  "scripts": {
    "test": "npm run test-browser && npm run test-node",
    "test-node": "jest --testEnvironment=node",
    "test-browser": "jest --testEnvironment=jsdom",
    "build": "rm -rf dist && tsc",
    "test-build-node": "jest --rootDir=dist --testEnvironment=node",
    "test-build-browser": "jest --rootDir=dist --testEnvironment=jsdom"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/project-ap/attestation-protocol-ts"
  },
  "author": "atz3n",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.14",
    "jest": "^24.9.0",
    "jest-cli": "^24.9.0",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "@somedotone/ardor-ts": "^2.2.3"
  }
}
